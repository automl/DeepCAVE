name: PyTest
on: [ push ]
jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Add conda to system path
        run: echo $CONDA/bin >> $GITHUB_PATH # $CONDA is an environment variable pointing to the root of the miniconda directory
      - name: Install dependencies
        run: conda env update --file environment.yml --name base
      - run: pytest tests
